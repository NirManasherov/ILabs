#include <iostream>

using namespace std; 

enum {BUFF_SIZE = 1000]; 

int main()
{
    char *buffer[BUFF_SIZE] = {0};
    
    while(true)
    {
        fgets(buffer, BUFF_SIZE , iostream);
    }
}   
